# AGENTS.md

## プロジェクト概要

このリポジトリは「アイ・ツー AI社員プロジェクト」の開発用リポジトリです。

最初の実装対象は、AI会議秘書 Ver.0.1です。

プロジェクトの目的は、次のとおりです。

> 人が仕事を探す会社から、AIが仕事を見つけ、人が判断する会社へ。

---

## 現在の作業

GitHub Issueに記載されたマイルストーン1の初期構成を実装します。

対象範囲は次のとおりです。

- Next.jsフロントエンドの作成
- FastAPIバックエンドの作成
- `GET /health` APIの作成
- フロントエンドから `/health` を呼び出す
- `.env.example` の作成
- `.gitignore` の整備
- READMEへの起動手順追記
- lint、型チェック、テスト、ビルドの実行

この段階では、次を実装しません。

- ログイン
- 会議登録
- データベースの業務テーブル
- AI API接続
- n8n連携
- Notion連携
- 本番デプロイ

---

## 開発環境

| 項目 | バージョン・方式 |
|---|---|
| Python | 3.12.1 |
| Node.js | 24.14.0 |
| npm | 11.9.0 |
| フロントエンド | Next.js、React、TypeScript |
| バックエンド | FastAPI |
| Python環境 | `venv` と `pip` |
| フロントエンド管理 | npm |
| Docker Compose | 初期構築では使用しない |

既存環境のバージョンを無断で変更しないでください。

---

## ディレクトリ構成

```text
i2-AI-employees/
├── frontend/
├── backend/
├── docs/
├── AGENTS.md
├── README.md
├── .env.example
└── .gitignore
```
必要以上にディレクトリや設定ファイルを追加しないでください。

---

## 実装ルール
1.最小構成で動作させる
2.一度に大きな変更を行わない
3.Issueの対象外機能を追加しない
4.既存の設計書を優先する
5.会議生データとAI生成データを将来分離できる構成にする
6.シークレットをソースコードへ記述しない
7..env をGitへコミットしない
8.エラーを握りつぶさない
9.型を明確にする
10.不要な依存パッケージを追加しない
11.日本語の説明文を基本とする
12.コード上の識別子は英語を使用する

## フロントエンド規則

Next.js App Routerを使用する
TypeScriptを使用する
any の使用を避ける
APIのURLは環境変数から取得する
API接続失敗時の表示を用意する
最初は画面装飾より動作確認を優先する
Client Componentは必要な箇所だけで使用する

品質確認コマンドは次のとおりです。

npm run lint
npm run typecheck
npm run build

typecheck スクリプトが存在しない場合は、次を追加します。

"typecheck": "tsc --noEmit"

## バックエンド規則

FastAPIを使用する
Pythonの型ヒントを付ける
APIの応答形式を明示する
設定値は環境変数から取得する
CORSの許可先を無制限にしない
/health は外部AIや外部サービスへ接続しない
/health は高速に応答する
ログへ秘密情報を出力しない

GET /health の応答形式は次のとおりです。

{
  "status": "ok",
  "service": "meeting-ai-backend",
  "version": "0.1.0",
  "environment": "local"
}

品質確認コマンドは次のとおりです。

ruff check .
pytest

## セキュリティ規則

次の情報をコミットしないでください。

APIキー
パスワード
セッショントークン
Notionトークン
n8n認証情報
.env
SQLiteデータベース
個人情報を含む会議データ
デバッグ用の秘密情報

.env.example には、値を入れず変数名だけを記載してください。

## Git規則

変更前に現在の差分を確認する
一つのコミットには一つの目的を持たせる
自動生成ファイルを必要以上にコミットしない
既存ファイルを無断で削除しない
設計書を無断で大幅変更しない
コミット前に品質確認を実行する

推奨コミットメッセージ例：

chore: フロントエンドとバックエンドの初期構成を作成
feat: health APIを追加
docs: ローカル起動手順を追加
test: health APIのテストを追加

## 作業手順

Codexは、実装開始時に次の順序で進めてください。
1.リポジトリと関連設計書を確認する
2.GitHub Issueの対象範囲を確認する
3.実装計画を簡潔に提示する
4.必要なファイルだけを変更する
5.各コマンドを実行して動作確認する
6.エラーが発生した場合は原因を説明して修正する
7.変更ファイルと確認結果を報告する
8.利用者の確認前にコミットやプッシュを行わない

## 完了条件

作業完了時には、次を報告してください。

- 作成・変更したファイル
- 実装した内容
- 実行したコマンド
- テスト結果
- lint結果
- 型チェック結果
- ビルド結果
- 残っている問題
- 次に必要な作業

利用者の明示的な指示がない限り、GitへのコミットやGitHubへのプッシュは行わないでください。